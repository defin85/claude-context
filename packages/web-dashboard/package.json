{
    "name": "@zilliz/claude-context-web-dashboard",
    "version": "0.1.6",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite --host 127.0.0.1",
        "build": "tsc --noEmit && vite build",
        "preview": "vite preview --host 127.0.0.1",
        "typecheck": "tsc --noEmit",
        "lint": "tsc --noEmit",
        "test": "tsx src/actionLog.test.ts && tsx src/searchDiagnostics.test.ts && tsx src/operationsView.test.ts && tsx src/workerTelemetry.test.ts && tsc --noEmit",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "vite": "^6.3.5"
    }
}
